[project]
name = "video-frame-extract"
version = "0.1.0"
description = "Extract the first frame of a video as JPEG via FFmpeg on AWS Lambda"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["boto3"]
